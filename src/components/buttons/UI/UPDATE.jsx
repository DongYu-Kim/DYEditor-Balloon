import Button from "@mui/material/Button";

export default function UPDATE({click}) {
    return <Button variant="contained" color="warning" style={{width: "100%"}} onClick={click}>"UPDATE"</Button>
}
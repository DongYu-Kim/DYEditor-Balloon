import Button from "@mui/material/Button";

export default function CREATE({click}) {
    return <Button variant="contained" color="primary" style={{width: "100%"}} onClick={click}>"CREATE"</Button>
}